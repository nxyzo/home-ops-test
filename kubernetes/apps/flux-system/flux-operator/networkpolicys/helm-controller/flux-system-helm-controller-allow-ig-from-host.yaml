apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: flux-system-helm-controller-allow-ig-from-host
  namespace: flux-system
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/component: helm-controller
  ingress:
    - fromEntities:
        - host
      toPorts:
        - ports:
            - protocol: TCP
              port: "9440"