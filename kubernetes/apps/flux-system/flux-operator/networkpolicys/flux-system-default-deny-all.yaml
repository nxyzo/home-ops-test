apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: flux-system-default-deny-all
  namespace: flux-system
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress: []