---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg-operator
  namespace: cnpg-system
spec:
  interval: 1h
  chart:
    spec:
      chart: cnpg-operator
      version: 1.28.1
      sourceRef:
        kind: HelmRepository
        name: cnpg-operator
        namespace: cnpg-system
  values:
    image:
      repository: ghcr.io/cnpg/cnpg-operator
      tag: 1.28.1